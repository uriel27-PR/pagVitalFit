<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VitalFit - Tu Tienda de Suplementos Deportivos</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* Variables globales */
        :root {
            --primary-color: #00c4b4;
            --secondary-color: #212121;
            --background-color: #000000;
            --text-color: #ffffff;
        }
        
        /* Reset y estilos base */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }
        
        body {
            background-color: var(--background-color);
            color: var(--text-color);
        }
        
        /* Header y navegación */
        header {
            background-color: var(--secondary-color);
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
        }
        
        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 5%;
        }
        
        .logo img {
            height: 40px;
        }
        
        /* Búsqueda y carrito */
        .search-cart {
            display: flex;
            align-items: center;
        }
        
        .search-form {
            display: flex;
            margin-right: 20px;
        }
        
        .search-form input {
            padding: 8px;
            border: none;
            background-color: #ffffff;
            color: #000000;
        }
        
        .search-form button {
            background-color: var(--primary-color);
            border: none;
            color: white;
            padding: 8px 15px;
            cursor: pointer;
        }
        
        .user-cart a {
            color: white;
            text-decoration: none;
            margin-left: 20px;
            font-size: 20px;
        }
        
        /* Navegación principal */
        nav {
            background-color: var(--secondary-color);
        }
        
        nav ul {
            display: flex;
            list-style-type: none;
            justify-content: center;
            padding: 10px 0;
        }
        
        nav ul li {
            position: relative;
            margin: 0 15px;
        }
        
        nav ul li a {
            color: var(--text-color);
            text-decoration: none;
            font-size: 16px;
            padding: 10px 15px;
            display: block;
            transition: color 0.3s;
        }
        
        nav ul li a:hover,
        .submenu a:hover {
            color: #cccccc;
        }
        
        /* Submenús */
        .submenu {
            display: none;
            position: absolute;
            top: 100%;
            left: 0;
            background-color: var(--secondary-color);
            padding: 10px;
            border-radius: 5px;
            z-index: 1000;
            min-width: 200px;
        }
        
        nav ul li:hover .submenu {
            display: block;
        }
        
        .submenu a {
            display: block;
            padding: 8px 10px;
            color: var(--text-color);
            text-decoration: none;
            font-size: 14px;
        }
        
        /* Barra de envío gratuito */
        .free-shipping-bar {
            background-color: var(--primary-color);
            color: white;
            text-align: center;
            padding: 5px 0;
            font-size: 0.9em;
            margin-top: 10px;
            position: relative;
            z-index: 999;
        }
        
        #terms-content {
            background-color: var(--primary-color);
            color: white;
            padding: 10px;
            text-align: center;
            font-size: 0.8em;
            max-width: 600px;
            margin: 0 auto;
            line-height: 1.4;
            display: none;
        }
        
        .terms-link {
            text-decoration: underline;
            cursor: pointer;
        }
        
        /* Carrusel */
        .carousel {
            margin-top: 120px;
            position: relative;
            width: 100%;
            overflow: hidden;
        }
        
        .carousel-inner {
            display: flex;
            transition: transform 0.5s ease;
        }
        
        .carousel-item {
            min-width: 100%;
            box-sizing: border-box;
        }
        
        .carousel-item img {
            width: 100%;
            height: auto;
        }
        
    /* Contenido principal */
    main {
        background-color: #ffffff;
        color: #000000;
        padding: 2rem 5%;
    }
    
    /* Títulos de sección */
    .productos h2 {
        text-align: center;
        margin: 2rem 0;
        font-size: 2rem;
        width: 100%;
        /* Asegura que el título ocupe todo el ancho */
    }
    
    /* Sección de productos */
    .productos {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        /* Cambiado a center para mejor alineación */
        gap: 20px;
        /* Espacio uniforme entre productos */
        margin-top: 2rem;
        width: 100%;
    }
    
    .producto {
        width: calc(20% - 20px);
        /* 20% para 5 productos en una fila, menos el gap */
        min-width: 200px;
        /* Ancho mínimo para evitar productos muy estrechos */
        margin-bottom: 2rem;
        text-align: center;
        transition: all 0.3s;
        flex-shrink: 0;
        /* Evita que los productos se encojan */
    }
    
    .producto img {
        max-width: 100%;
        height: 200px;
        object-fit: cover;
    }
    
    .producto h3 {
        margin: 1rem 0;
        font-size: 1.2rem;
    }
    
    .producto .marca {
        font-size: 0.9rem;
        color: #666;
    }
    
    .producto .precio {
        color: var(--primary-color);
        font-weight: bold;
        margin-bottom: 0.5rem;
        font-size: 1.2rem;
    }
    
    .producto button {
        background-color: var(--primary-color);
        border: none;
        color: white;
        padding: 0.5rem 1rem;
        cursor: pointer;
        border-radius: 4px;
    }
        
        /* Títulos de sección */
        h2 {
            text-align: center;
            margin: 2rem 0;
            font-size: 2rem;
        }
        
        /* Marcas populares */
        .marcas-populares {
            text-align: center;
            margin-top: 3rem;
            padding: 2rem 0;
            background-color: #f8f8f8;
        }
        
        .marcas-grid {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 2rem;
        }
        
        .marca-logo {
            max-width: 150px;
            height: auto;
        }
        
        /* Banners de promociones */
        .promociones-banner {
            display: flex;
            justify-content: space-between;
            margin-top: 2rem;
        }
        
        .promocion-banner {
            width: 48%;
        }
        
        .promocion-banner img {
            width: 100%;
            height: auto;
        }
        
        /* Botones y elementos interactivos */
        .btn-primary {
            display: block;
            width: 100%;
            padding: 12px;
            margin-top: 20px;
            background-color: var(--primary-color);
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 4px;
            font-size: 16px;
            transition: background-color 0.3s;
            text-align: center;
            text-decoration: none;
        }
        
        .btn-primary:hover {
            background-color: #00a396;
        }
        
        /* Footer */
        .footer-container {
            background-color: var(--background-color);
            color: var(--text-color);
            padding: 40px 5%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        
        .subscribe-section {
            width: 100%;
            max-width: 400px;
            margin-bottom: 30px;
        }
        
        .subscribe-form {
            display: flex;
            flex-direction: column;
            width: 100%;
        }
        
        .subscribe-form input[type="email"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: none;
        }
        
        .subscribe-form button {
            width: 100%;
            padding: 10px;
            background-color: var(--primary-color);
            color: white;
            border: none;
            cursor: pointer;
            text-transform: uppercase;
        }
        
        .footer-links {
            display: flex;
            justify-content: space-between;
            width: 100%;
            max-width: 1000px;
        }
        
        .footer-section {
            flex: 0 1 auto;
            text-align: left;
            margin: 0 10px;
        }
        
        .footer-section h3 {
            font-size: 1.2em;
            margin-bottom: 15px;
        }
        
        .footer-section ul {
            list-style-type: none;
        }
        
        .footer-section ul li {
            margin-bottom: 10px;
        }
        
        .footer-section ul li a {
            color: var(--text-color);
            text-decoration: none;
        }
        
        #cart-modal {
    display: none;
    position: fixed;
    z-index: 1001;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0,0,0,0.5);
    overflow: auto;
}

.modal-content {
    background-color: #fefefe;
    margin: 5% auto;
    padding: 20px;
    border: 1px solid #888;
    width: 80%;
    max-width: 800px;
    position: relative;
}

/* Estilos para el encabezado del carrito */
.cart-header {
    display: flex;
    align-items: center;
    margin-bottom: 20px;
}

.cart-header i {
    font-size: 24px;
    margin-right: 10px;
    color: var(--primary-color);
}
.cart-header h2 {
    color: #000000;
    margin: 0;
    font-size: 24px;
}

        /* Estilos para la tabla del carrito */
#cart-items {
    width: 100%;
    margin: 20px 0;
}

#cart-items table {
    width: 100%;
    border-collapse: collapse;
}

#cart-items th {
    background-color: #f2f2f2;
    color: #000000;
    font-weight: bold;
    padding: 12px;
    text-align: left;
    border-bottom: 2px solid #ddd;
}

#cart-items td {
    padding: 12px;
    border-bottom: 1px solid #ddd;
    color: #000000;
}

  /* Estilos para el botón de vaciar carrito */
.btn-clear {
    background-color: #ffffff;
    color: var(--primary-color);
    border: 1px solid var(--primary-color);
    padding: 8px 12px;
    cursor: pointer;
    float: right;
    margin-bottom: 10px;
    border-radius: 4px;
    transition: background-color 0.3s, color 0.3s;
    display: flex;
    align-items: center;
    gap: 5px;
}

.btn-clear i {
    font-size: 16px;
}

.btn-clear:hover {
    background-color: var(--primary-color);
    color: #ffffff;
}

    /* Estilos para el total y botón de pago */
#cart-total {
    color: #000000;
    font-size: 18px;
    font-weight: bold;
    text-align: right;
    margin: 20px 0;
}

.btn-primary {
    width: 100%;
    padding: 12px;
    background-color: var(--primary-color);
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 16px;
}
/* Estilos para el botón de cerrar */
.close {
    color: #aaa;
    float: right;
    font-size: 28px;
    font-weight: bold;
    cursor: pointer;
}

.close:hover {
    color: #000;
}

/* Estilos para los campos de cantidad */
.quantity-input {
    width: 50px;
    padding: 5px;
    text-align: center;
    border: 1px solid #ddd;
    border-radius: 4px;
}
/* Estilos para el botón de eliminar item */
.remove-item {
    color: #ff0000;
    cursor: pointer;
    background: none;
    border: none;
    font-size: 18px;
    padding: 5px;
}

.remove-item:hover {
    color: #cc0000;
}
       
        
        /* Responsive */
        @media (max-width: 768px) {
            .footer-links {
                flex-direction: column;
                align-items: center;
            }
            
            .footer-section {
                margin: 20px 0;
                text-align: center;
            }
            
            .footer-section.nosotros {
                margin-left: 0;
            }
            
            .producto {
                width: calc(50% - 20px);
            }
        }

        .mensaje {
    position: fixed;
    top: 20px;
    right: 20px;
    padding: 15px;
    border-radius: 5px;
    z-index: 1000;
}

.mensaje.success {
    background-color: #4CAF50;
    color: white;
}

.mensaje.error {
    background-color: #f44336;
    color: white;
}

.quantity-controls {
    display: flex;
    align-items: center;
    gap: 10px;
}

.quantity-controls button {
    padding: 5px 10px;
    border: none;
    background-color: #ddd;
    cursor: pointer;
}

.quantity-controls button:hover {
    background-color: #ccc;
}
    </style>

<head>
<body>
    <!-- Header -->
    <header>
        <div class="top-bar">
            <!-- Logo -->
            <div class="logo">
                <img src="img/logoVitalfit.webp" alt="VitalFit">
            </div>
            
            <!-- Search and Cart -->
            <div class="search-cart">
                <form class="search-form" id="search-form">
                    <input type="text" placeholder="Buscar" id="search-input">
                    <button type="submit"><i class="fas fa-search"></i></button>
                </form>

                <!-- Search Results Container -->
                <div id="search-results" style="display: none;">
                    <div>
                        <h2>Resultados de búsqueda para '<span id="search-term"></span>'</h2>
                        <button id="close-search">×</button>
                    </div>
                    <p><span id="results-count"></span> productos</p>
                    <hr>
                    <div id="results-container"></div>
                </div>

                <!-- User Profile and Cart -->
                <div class="user-cart">
                    <a href="/iniciarsesion"><i class="fas fa-user"></i> Mi Perfil</a>
                    <a href="#" id="cart-icon">
                        <i class="fas fa-shopping-cart"></i>
                        <span id="cart-count">0</span>
                    </a>
                    <div>
                        <a href="/opcionescuenta">⚙ Opciones de Cuenta</a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Main Navigation -->
        <nav>
            <ul></ul>
        </nav>

        <!-- Free Shipping Banner -->
        <div class="free-shipping-bar">
            <p>¡ENVÍOS GRATIS! <span class="terms-link">Ver términos</span></p>
            <div id="terms-content" style="display: none;">
                <p>En la compra de más de $1,600 pesos el envío es completamente GRATIS (por estafeta), no importa el peso, ciudad, o si eres cliente mayorista.</p>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main>
        <!-- Carousel -->
        <section class="carousel">
            <div class="carousel-inner">
                <div class="carousel-item"><img src="img/presentacionPreentreno.gif" alt="Pre-entreno"></div>
                <div class="carousel-item"><img src="img/presentacionCarnitina.gif" alt="Carnitina"></div>
                <div class="carousel-item"><img src="img/presentacionNew.gif" alt="Creatina"></div>
                <div class="carousel-item"><img src="img/presentacionProteina.gif" alt="Proteina"></div>
                <div class="carousel-item"><img src="img/presentacionSleepXS.gif" alt="SleepXS"></div>
            </div>
        </section>

        <!-- Featured Products -->
        <section class="productos" id="productos-destacados">
            <h2>Productos Destacados</h2>
        </section>

        <!-- Promotions -->
        <section class="productos" id="productos-promociones">
            <h2>Promociones</h2>
        </section>

        <!-- Popular Brands -->
        <section class="marcas-populares">
            <h2>Marcas más populares</h2>
            <div class="marcas-grid"></div>
            <div class="promociones-banner">
                <div class="promocion-banner">
                    <img src="/img/descuentosmarcas2.gif" alt="Promoción King Mass">
                </div>
                <div class="promocion-banner">
                    <img src="/img/descuentosmarcas2.gif" alt="Descuentos para mayoristas">
                </div>
            </div>
        </section>
    </main>

    <!-- Footer -->
    <footer class="footer-container">
        <!-- Newsletter -->
        <div class="subscribe-section">
            <h3>BOLETIN</h3>
            <p>Suscríbete y recibe promociones</p>
            <form class="subscribe-form" id="boletinForm" action="/suscribir-boletin" method="POST">
                <input type="email" name="email" placeholder="Email" required>
                <button type="submit">SUSCRÍBETE</button>
            </form>
        </div>

        <!-- Footer Links -->
        <div class="footer-links">
            <div class="footer-section">
                <h3>DUDAS</h3>
                <ul>
                    <li><a href="#">Realizar Pedido</a></li>
                    <li><a href="#">Envíos</a></li>
                    <li><a href="#">Pagos</a></li>
                </ul>
            </div>

            <div class="footer-section help-section">
                <h3>NECESITAS AYUDA</h3>
                <p>Lunes a Sábado 8 am a 7 pm</p>
                <div class="chat-email">
                    <i class="chat-icon"></i>
                </div>
            </div>

            <div class="footer-section">
                <h3>NOSOTROS</h3>
                <ul>
                    <li><a href="#">Quiénes somos</a></li>
                    <li><a href="#">Términos</a></li>
                    <li><a href="#">Privacidad</a></li>
                </ul>
            </div>
        </div>
    </footer>

    <!-- Shopping Cart Modal -->
    <div id="cart-modal" class="modal">
        <div class="modal-content">
            <span class="close">×</span>
            <div class="cart-header">
                <i class="fas fa-shopping-cart"></i>
                <h2>Carrito de compras</h2>
            </div>
            <button id="clear-cart" class="btn-clear">
                <i class="fas fa-trash"></i>
                Vaciar Carrito
            </button>
            <div id="cart-items">
                <table>
                    <thead>
                        <tr>
                            <th>Nombre del producto</th>
                            <th>Precio</th>
                            <th>Cantidad</th>
                            <th>Sub-total</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody id="cart-items-body"></tbody>
                </table>
            </div>
            <p id="cart-total">Total: $0.00</p>
            <button class="btn-primary">Proceder al pago</button>
        </div>
        <div class="carrito-container">
            <% if (typeof productos === 'undefined') { %>
                <p>Error: No se han cargado los productos</p>
            <% } else if (productos.length === 0) { %>
                <p>No hay productos disponibles en este momento</p>
            <% } else { %>
                <% productos.forEach(function(producto) { %>
                    <div class="item-carrito" data-producto-id="<%= producto.id %>" data-precio="<%= producto.precio %>">
                        <img src="<%= producto.imagen %>" alt="<%= producto.nombre %>">
                        <h3><%= producto.nombre %></h3>
                        <p>Precio: $<%= producto.precio %></p>
                        <input type="number" class="cantidad-input" value="1" min="1">
                        <button class="eliminar-producto">Eliminar</button>
                    </div>
                <% }); %>
            <% } %>
        </div>
    </div>
</body>
</head>

    <script>
    document.addEventListener('DOMContentLoaded', function() {
        // Cart Management
        let cart = [];
        const cartElements = {
            icon: document.getElementById('cart-icon'),
            modal: document.getElementById('cart-modal'),
            closeBtn: document.querySelector('#cart-modal .close'),
            clearBtn: document.getElementById('clear-cart'),
            count: document.getElementById('cart-count'),
            itemsBody: document.getElementById('cart-items-body')
        };
    
        // Search Elements
        const searchElements = {
            form: document.getElementById('search-form'),
            input: document.getElementById('search-input'),
            results: document.getElementById('search-results'),
            closeBtn: document.getElementById('close-search')
        };
    
        // Initialize Application
        function initApp() {
            // Load Products and Brands
            loadProducts('productos-destacados', productosDestacados);
            loadProducts('productos-promociones', productosPromociones);
            loadBrands();
    
            // Set up Event Listeners
            setupEventListeners();
    
            // Initialize Carousel
            initCarousel();
        }
    
        // Set up Event Listeners
        function setupEventListeners() {
            // Cart Interactions
            cartElements.icon.addEventListener('click', toggleCart);
            cartElements.closeBtn.addEventListener('click', closeCart);
            cartElements.clearBtn.addEventListener('click', clearCart);
    
            // Search Interactions
            searchElements.form.addEventListener('submit', handleSearch);
            searchElements.closeBtn.addEventListener('click', closeSearch);
    
            // Newsletter Subscription
            const boletinForm = document.getElementById('boletinForm');
            boletinForm.addEventListener('submit', subscribeNewsletter);
    
            // Terms Link
            const termsLink = document.querySelector('.terms-link');
            termsLink.addEventListener('click', toggleTerms);
        }
    
        // Carousel Initialization
        function initCarousel() {
            const carouselInner = document.querySelector('.carousel-inner');
            const carouselItems = document.querySelectorAll('.carousel-item');
            let currentIndex = 0;
    
            function showSlide(index) {
                carouselInner.style.transform = `translateX(-${index * 100}%)`;
            }
    
            // Auto-slide every 5 seconds
            setInterval(() => {
                currentIndex = (currentIndex + 1) % carouselItems.length;
                showSlide(currentIndex);
            }, 5000);
        }
    
        // Product Loading
        function loadProducts(containerId, products) {
            const container = document.getElementById(containerId);
            products.forEach(product => {
                const productElement = createProductElement(product);
                container.appendChild(productElement);
            });
        }
    
        // Create Product Element
        function createProductElement(product) {
            const div = document.createElement('div');
            div.className = 'producto';
            div.innerHTML = `
                <img src="${product.image}" alt="${product.name}">
                <p class="marca">${product.brand}</p>
                <h3>${product.name}</h3>
                <p class="precio">$${product.price.toFixed(2)} MXN</p>
                <button>Agregar al carrito</button>
            `;
            div.querySelector('button').addEventListener('click', () => addToCart(product));
            return div;
        }
    
        // Brands Loading
        function loadBrands() {
            const brandsContainer = document.querySelector('.marcas-grid');
            marcasPopulares.forEach(brand => {
                const img = document.createElement('img');
                img.src = brand.logo;
                img.alt = brand.name;
                img.className = 'marca-logo';
                brandsContainer.appendChild(img);
            });
        }
    
        // Cart Management Functions
        function addToCart(product) {
            const existingItem = cart.find(item => item.id === product.id);
            if (existingItem) {
                existingItem.quantity++;
            } else {
                cart.push({...product, quantity: 1});
            }
            updateCartCount();
            updateCartDisplay();
        }
    
        function removeFromCart(index) {
            cart.splice(index, 1);
            updateCartCount();
            updateCartDisplay();
        }
    
        function clearCart() {
            cart = [];
            updateCartCount();
            updateCartDisplay();
        }
    
        function updateCartCount() {
            const cartCount = cart.reduce((total, item) => total + item.quantity, 0);
            cartElements.count.textContent = cartCount;
        }
    
        function updateCartDisplay() {
            if (!cartElements.itemsBody) return;
    
            cartElements.itemsBody.innerHTML = '';
            let total = 0;
    
            if (cart.length === 0) {
                document.getElementById('cart-total').textContent = 'Total: $0.00';
                return;
            }
    
            cart.forEach((item, index) => {
                const row = document.createElement('tr');
                row.innerHTML = `
                    <td style="text-align: left; padding: 10px;">${item.name}</td>
                    <td style="text-align: center; padding: 10px;">$${item.price.toFixed(2)}</td>
                    <td style="text-align: center; padding: 10px;">
                        <input type="number" class="quantity-input" value="${item.quantity}" min="1" data-index="${index}">
                    </td>
                    <td style="text-align: center; padding: 10px;">$${(item.price * item.quantity).toFixed(2)}</td>
                    <td style="text-align: center; padding: 10px;">
                        <button class="remove-item" data-index="${index}">
                            <i class="fas fa-times"></i>
                        </button>
                    </td>
                `;
                cartElements.itemsBody.appendChild(row);
                total += item.price * item.quantity;
            });
    
            document.getElementById('cart-total').textContent = `Total: $${total.toFixed(2)}`;
    
            // Add event listeners for quantity inputs and remove buttons
            cartElements.itemsBody.querySelectorAll('.quantity-input').forEach(input => {
                input.addEventListener('change', (e) => updateQuantity(
                    parseInt(e.target.dataset.index), 
                    parseInt(e.target.value)
                ));
            });
    
            cartElements.itemsBody.querySelectorAll('.remove-item').forEach(button => {
                button.addEventListener('click', (e) => removeFromCart(
                    parseInt(e.target.closest('button').dataset.index)
                ));
            });
        }
    
        function updateQuantity(index, newQuantity) {
            cart[index].quantity = Math.max(1, parseInt(newQuantity));
            updateCartDisplay();
        }
    
        function toggleCart() {
            const isVisible = cartElements.modal.style.display === 'block';
            cartElements.modal.style.display = isVisible ? 'none' : 'block';
            updateCartDisplay();
        }
    
        function closeCart() {
            cartElements.modal.style.display = 'none';
        }
    
        // Search Functionality
        function handleSearch(e) {
            e.preventDefault();
            const query = searchElements.input.value.toLowerCase().trim();
            if (query === '') return;
    
            const allProducts = [...productosDestacados, ...productosPromociones];
            const results = allProducts.filter(product => 
                product.name.toLowerCase().includes(query) || 
                product.brand.toLowerCase().includes(query)
            );
    
            displaySearchResults(query, results);
        }
    
        function displaySearchResults(query, results) {
            document.getElementById('search-term').textContent = query;
            document.getElementById('results-count').textContent = results.length;
    
            const resultsContainer = document.getElementById('results-container');
            resultsContainer.innerHTML = '<hr>';
    
            results.forEach(product => {
                const productElement = createProductElement(product);
                productElement.style.width = '200px';
                productElement.style.margin = '10px';
                resultsContainer.appendChild(productElement);
            });
    
            searchElements.results.style.display = 'block';
            searchElements.results.style.maxHeight = '80vh';
            searchElements.results.style.overflowY = 'auto';
            document.querySelector('.user-cart').style.display = 'none';
        }
    
        function closeSearch() {
            searchElements.results.style.display = 'none';
            document.querySelector('.user-cart').style.display = 'block';
            searchElements.input.value = '';
        }
    
        // Newsletter Subscription
        async function subscribeNewsletter(e) {
            e.preventDefault();
            const emailInput = e.target.querySelector('input[name="email"]');
            const email = emailInput.value;
    
            try {
                const response = await fetch('/suscribir-boletin', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ email })
                });
    
                const responseText = await response.text();
                if (response.ok) {
                    alert('¡Gracias por suscribirte a nuestro boletín!');
                    emailInput.value = ''; // Clear the input
                } else {
                    console.error('Error response:', responseText);
                    alert(responseText || 'Hubo un problema al suscribirse');
                }
            } catch (error) {
                console.error('Fetch Error:', error);
                alert('Hubo un problema al suscribirse');
            }
        }
    
        // Terms and Conditions Toggle
        function toggleTerms(e) {
            e.preventDefault();
            const termsContent = document.getElementById('terms-content');
            termsContent.style.display = termsContent.style.display === 'none' || termsContent.style.display === '' 
                ? 'block' 
                : 'none';
        }

        // Ejemplo de agregar al carrito
async function agregarAlCarrito(productoId, cantidad, precio) {
    try {
        const usuarioId = obtenerIdUsuarioActual(); // Implementa esta función para obtener el ID del usuario logueado
        
        const respuesta = await fetch('/agregar-carrito', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({
                usuarioId,
                productoId,
                cantidad,
                precio
            })
        });
        
        const datos = await respuesta.json();
        
        if (respuesta.ok) {
            alert('Producto agregado al carrito');
            cargarCarrito(); // Función para recargar el carrito
        } else {
            throw new Error(datos.message);
        }
    } catch (error) {
        console.error('Error:', error);
        alert('No se pudo agregar el producto al carrito');
    }
}

// Ejemplo de cargar carrito
async function cargarCarrito() {
    try {
        const usuarioId = obtenerIdUsuarioActual();
        const respuesta = await fetch(`/carrito/${usuarioId}`);
        const productos = await respuesta.json();
        
        // Renderizar productos en el carrito
        const carritoElemento = document.getElementById('carrito-lista');
        carritoElemento.innerHTML = '';
        
        productos.forEach(producto => {
            const elementoProducto = document.createElement('div');
            elementoProducto.innerHTML = `
                ${producto.nombre} - Cantidad: ${producto.cantidad} - $${producto.subtotal}
                <button onclick="eliminarDelCarrito(${producto.id})">Eliminar</button>
            `;
            carritoElemento.appendChild(elementoProducto);
        });
    } catch (error) {
        console.error('Error al cargar carrito:', error);
    }
}
    
        // Product and Brand Data (would typically come from an API)
        const productosDestacados = [
            { id: 1, name: "Whey Protein 100% Isolate 1 Kg", brand: "SD Nutrition", price: 199.00, image: "/img/productoDest1.jpg" },
            { id: 2, name: "Psychotic 205 Gr", brand: "Insane Labz", price: 307.00, image: "img/productoDestc2.jpg" },
            { id: 3, name: "Caffeine 100 Tabs", brand: "GAT Sport", price: 129.00, image: "img/productoDest3.jpg" },
            { id: 4, name: "Boost Creatina 260 Gr", brand: "MuscleFit", price: 197.00, image: "img/productoDestc4.jpg" },
            { id: 5, name: "Hydroxycut Hardcore Elite 110 Caps", brand: "MuscleTech", price: 367.00, image: "img/productoDestc5.jpg" }
        ];
    
        const productosPromociones = [
            { id: 6, name: "Whey Protein HD 4 Lb", brand: "BPI Sports", price: 649.00, image: "img/promocion1.jpg" },
            { id: 7, name: "Creatine XS 300 Gr", brand: "Ronnie Coleman", price: 254.00, image: "img/promocion2.jpg" },
            { id: 8, name: "Colageno Hidrolizado 500 Gr", brand: "Meta Nutrition", price: 240.00, image: "img/promocion3.jpg" },
            { id: 9, name: "The Original BCAA 390 Gr", brand: "Xtend", price: 265.00, image: "img/promocion4.jpg" },
            { id: 10, name: "Beta-Alanine 101 Gr", brand: "Insane Labz", price: 134.00, image: "img/promocion5.png" }
        ];
    
        const marcasPopulares = [
            { name: "MuscleTech", logo: "/img/marca1.png" },
            { name: "HTP", logo: "/img/marca2.png" },
            { name: "MuscleMeds", logo: "/img/marca3.png" },
            { name: "Insane Labz", logo: "/img/marca4.png" },
            { name: "Ronnie Coleman", logo: "img/marca5.png" },
            { name: "Mutant", logo: "img/marca6.png" },
            { name: "GAT Sport", logo: "img/marca7.png" },
            { name: "Nutrex Research", logo: "img/marca8.png" }
        ];
    
        // Initialize the application
        initApp();
    });
    </script>